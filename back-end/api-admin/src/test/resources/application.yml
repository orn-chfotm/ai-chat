spring:
  datasource:
    url: jdbc:h2:mem:chatai;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false

app:
  file-storage:
    base-dir: ./build/test-uploads
  vectordb:
    elasticsearch:
      url: http://localhost:9200
