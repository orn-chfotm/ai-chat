/**
 * Elasticsearch adapter for {@link com.learn.chatai.domain.policy.port.PolicyVectorStorePort} (PRD 04).
 * Chunk generation, embedding, and index provisioning are follow-up work for the vectorization
 * milestone — this package only wires the storage/search contract.
 */
package com.learn.chatai.infra.vectordb;
